server:
  servlet:
    port: 8081
    contextPath: /api

spring:

  application:
    name: proman-api

  datasource:
    url: jdbc:postgresql://localhost:5432/promandb
    username: postgres
    password: password

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
